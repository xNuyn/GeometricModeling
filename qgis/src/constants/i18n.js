export const BUTTON = Object.freeze({
  LOGIN: 'login',
  LOGOUT: 'logout',
})

export const LAYOUT_LOGIN = Object.freeze({
  LOGIN: 'login',
  LOGOUT: 'logout',
})

export const MENU_ADMIN = Object.freeze({
  LOGIN: 'login',
  LOGOUT: 'logout',
})

export const LAYER = Object.freeze({
  LOGIN: 'login',
  LOGOUT: 'logout',
})